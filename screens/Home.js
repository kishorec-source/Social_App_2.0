import React from "react";
import { View, Text } from "react-native";

const Home = () => {
  return (
    <View>
      <Text>Welcome to Home Screen</Text>
      {/* Add components for posts, ads, notifications */}
    </View>
  );
};

export default Home;
